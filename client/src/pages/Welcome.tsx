import { Stack, Typography } from "@mui/material"

const Welcome = () => {
    return <Stack
        justifyContent={'center'}
        alignItems={'center'}
        flexGrow={1}
    >
        <Typography variant="h4" >Welcome to Thuyiya</Typography>
    </Stack>
}

export default Welcome